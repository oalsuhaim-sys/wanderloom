# Client ID route slot

The active route is **`src/app/welcome/client/[clientId]/page.tsx`**.

This folder intentionally uses a static name (not `[clientId]`) to avoid conflicting with other dynamic segments under `/welcome`.
